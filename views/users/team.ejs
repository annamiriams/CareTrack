<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Team</title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="title">
        <h1>
            Team
        </h1>
        <h4>
            <i>Click on any name below to see their referrals.</i>
        </h4>
    </div>

    <div id="referrals">
        <ul>
            <% users.forEach((user)=> { %>
                <li>
                    <a href="/users/<%= user._id %>">
                        <%= user.name.firstName %>
                        <%= user.name.lastName %>
                    </a>
                </li>
            <% }); %>
        </ul>
    </div>

</body>

<footer>
</footer>

</html>