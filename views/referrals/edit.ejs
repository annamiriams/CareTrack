<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Edit Referral</title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="title">
        <img src="/images/zamioculcas.png" alt="">
        <h1>Edit</h1>
    </div>

    <div>
        <p>Name: <%= referral.name %>
        </p>
        <p>DOB: <%= referral.birthday.toLocaleDateString('en-US') %>
        </p>
    </div>
    
    <form action="/users/<%= user._id %>/referrals/<%= referral._id %>?_method=PUT" method="POST">

        <fieldset>
            <legend>Enter info:</legend>
            <div>
                <label for="name">Name:</label>
                <input type="text" name="name" id="name" pattern="[A-Za-z]+" value="<%= referral.name %>">
            </div>

            <div>
                <label for="birthday">Birthday:</label>
                <input type="date" name="birthday" id="birthday" value="<%= referral.birthday %>">
            </div>
            
            <div class="button">
                <button type="submit" class="button">Save Referral</button>
            </div>
        </fieldset>


    </form>
</body>

<footer>
    <a href="https://www.flaticon.com/free-icons/plant" title="plant icons">Plant icons created by surang.</a>
</footer>

</html>