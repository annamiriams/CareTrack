<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Edit Referral</title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit a Referral</h1>
    <h3><%= referral.name %></h3>

    <form action="/users/<%= user._id %>/referrals/<%= referral._id %>?_method=PUT" method="POST">

        <fieldset>
            <legend>Enter info:</legend>
            <div>
                <label for="name">Name:</label>
                <input type="text" name="name" id="name" value="<%=referral.name %>">
            </div>

            <div>
                <label for="birthday">Birthday:</label>
                <input type="text" name="birthday" id="birthday" value="<%= referral.birthday %>">
            </div>

            <div>
                <label for="insurance">Insurance:</label>
                <select id="insurance" name="insurance" required>
                    <option value="medicaid" <%= referral.insurance === 'medicaid' ? 'selected' : '' %>>Medicaid</option>
                    <option value="kaiser" <%= referral.insurance === 'kaiser' ? 'selected' : '' %>>Kaiser</option>
                    <option value="pacificsource" <%= referral.insurance === 'pacificsource' ? 'selected' : '' %>>PacificSource</option>
                    <option value="regence" <%= referral.insurance === 'regence' ? 'selected' : '' %>>Regence</option>
                    <option value="optum" <%= referral.insurance === 'optum' ? 'selected' : '' %>>Optum</option>
                    <option value="unknown" <%= referral.insurance === 'unknown' ? 'selected' : '' %>>Unknown</option>
                </select>

            <button type="submit">Update</button>

            </div>

        </fieldset>


    </form>
</body>

</html>