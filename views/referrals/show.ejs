<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Referral Overview</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= referral.name %></h1>

    <div>
        <p>Name: <%= referral.name %> </p>
        <p>DOB: <%= referral.birthday %></p>
    </div>

    <!-- if insurance has been entered for the selected referral, it will populate on the page. otherwise, it will not show any insurance information. -->
    <!-- insurance is lowercase because of enum/camel case, so in the show page i've added 'referral.insurance.charAt(0).toUpperCase() + referral.insurance.slice(1);' to make the first letter capital. this doesnt fix pacsrc though. is there a better way of doing this? it seems like there should be a way other than hardcoding it every single time 'insurance' is shown on the page... -->
    <div>
        <% if (referral.insurance) { %>
            <p>Insurance: <%= referral.insurance.charAt(0).toUpperCase() + referral.insurance.slice(1); %></p>
        <% } %>
    </div>

    <div>
        <% if (referral.insuranceConfirmed) { %>
            <p><%= referral.insurance.charAt(0).toUpperCase() + referral.insurance.slice(1); %> was confirmed on <%= referral.dateInsuranceConfirmed %></p>
        <% } else { %>
            <p>Insurance has not been confirmed.</p>
        <% } %>
    </div>

        <!-- always available -->
        
        <!-- edit button -->
        <form action="/users/<%= user._id %>/referrals/<%= referral._id %>/edit" method="GET">
            <button type="submit">Edit Referral</button>
        </form>

        <!-- confirm insurance button -->
        <form action="/users/<%= user._id %>/referrals/<%= referral._id %>/insurance" method="GET">
            <button type="submit">Confirm Insurance</button>
        </form>
        
        <!-- delete button -->
        <form action="/users/<%= user._id %>/referrals/<%= referral._id %>?_method=DELETE" method="POST">
            <button type="submit">Close Referral</button>
        </form>

        <!-- only available if... -->
        
        <!-- totally not tested yet!! -->
        <% if (referral.insuranceConfirmed) { %>
            <p>there will be a "schedule intake" button here</p>
            <!-- schedule intake button -->
        <% } %>

        <% if (referral.intakeScheduled) { %>
            <p>there will be a "complete intake" button here</p>
            <!-- complete intake button -->
        <% } %>

        <% if (referral.intakeCompleted) { %>
            <p>there will be a "close referral" button here</p>
            <!-- close referral (delete) button -->
            <% } %>

    </div>
</body>
</html>